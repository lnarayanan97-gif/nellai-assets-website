const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('nav');if(menuBtn){menuBtn.addEventListener('click',()=>{nav.style.display=nav.style.display==='flex'?'none':'flex';nav.style.flexDirection='column';nav.style.position='absolute';nav.style.top='76px';nav.style.left='0';nav.style.right='0';nav.style.background='#fff';nav.style.padding='20px';nav.style.boxShadow='0 10px 20px rgba(0,0,0,.08)'})}const searchBtn=document.getElementById('searchBtn');const type=document.getElementById('type');const area=document.getElementById('area');const cards=[...document.querySelectorAll('.property-card')];const noResults=document.getElementById('noResults');function filter(){const selected=type.value;const location=area.value.trim().toLowerCase();let count=0;cards.forEach(card=>{const ok=selected==='all'||card.dataset.type===selected;card.style.display=ok?'block':'none';if(ok)count++});noResults.hidden=count!==0;if(location){document.getElementById('properties').scrollIntoView({behavior:'smooth'})}}searchBtn?.addEventListener('click',filter);document.getElementById('contactForm')?.addEventListener('submit',e=>{e.preventDefault();document.getElementById('formMessage').textContent='Thank you. Your enquiry is ready. Please connect WhatsApp or call us to complete the enquiry.'});